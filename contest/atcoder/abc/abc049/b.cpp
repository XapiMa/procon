#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i = 0; i < (int)(n); i++)
#define REP(i,k,n) for(int i = k; i < (int)(n); i++)
typedef long long ll;

int main(){
        int h,w;
        string s;

        cin >> h >> w;
        rep(i,h){
                cin >> s;
                cout << s << endl;
                cout << s << endl;
        }
        return 0;
}
